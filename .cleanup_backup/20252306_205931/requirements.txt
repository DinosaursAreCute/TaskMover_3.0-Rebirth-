# Required Python packages
# All dependencies below are required for both runtime and test automation
# UI tests are automatically skipped in headless environments (no display)
ttkbootstrap
PyYAML
Colorlog

# Test requirements
pytest
pytest-mock
mock